   <tr>
        <th>{{jersey_num}}</th>
        <th><a href="/player/{{id}}">{{user.first_Name}} {{user.last_Name}}</a></th>
        <th>{{goals}}</th>
        <th>{{assists}}</th>
        <th>{{penalty_minutes}}</th>
    </tr>